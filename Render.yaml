services:
  - type: web
    name: geointel-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_API_KEY_1services:
  - type: web
    name: geointel-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_API_KEY_1
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: TAVILY_API_KEY_1
        sync: false
      - key: RENDER
        value: true
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: TAVILY_API_KEY_1
        sync: false
      - key: RENDER
        value: true